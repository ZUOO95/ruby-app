<%= form_with(model: team, local: true) do |form| %>
  <% if team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team.errors.count, "error") %> prohibited this team from being saved:</h2>

      <ul>
      <% team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :team_name %>
  </div>

  <div class="field">
    <%= form.label :skrot %>
    <%= form.text_field :skrot, id: :team_skrot %>
  </div>

  <div class="field">
    <%= form.label :pochodzenie %>
    <%= form.text_field :pochodzenie, id: :team_pochodzenie %>
  </div>

  <div class="field">
    <%= form.label :sponsorzy %>
    <%= form.text_field :sponsorzy, id: :team_sponsorzy %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
